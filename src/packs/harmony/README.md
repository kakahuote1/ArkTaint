# Harmony Built-in Semantic Packs

This directory contains the built-in Harmony semantic packs.

Harmony-specific hardcoded modeling logic now lives under `src/packs/harmony/**`.
`src/core/harmony` no longer carries the implementation body for these semantics.
