This folder is reserved for LLM-augmented **project-layer** rule bundles.

- Files under this directory are auto-loaded (if present) as part of the project layer.
- Place one or more `*.rules.json` files here (either directly or in subfolders).

