This folder is reserved for LLM-augmented **kernel-layer** rule bundles.

- Files under this directory are auto-loaded (if present) after the built-in kernel rules.
- Place one or more `*.rules.json` files here (either directly or in subfolders).

