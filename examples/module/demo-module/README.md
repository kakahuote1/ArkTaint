# Demo Module

This is a minimal Phase 8 module example.

Use it with:

```bash
node out/cli/analyze.js --repo <repo> --sourceDir . --module-root examples/module/demo-module
```

This demo no longer uses `manifest/providers/rules`. The pack is just a normal TypeScript file in the pack directory.
