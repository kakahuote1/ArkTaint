# Demo Semantic Pack

This is a minimal Phase 8 semantic pack example.

Use it with:

```bash
node out/cli/analyze.js --repo <repo> --sourceDir . --packs examples/semantic-pack/demo-pack
```

This demo no longer uses `manifest/providers/rules`. The pack is just a normal TypeScript file in the pack directory.
