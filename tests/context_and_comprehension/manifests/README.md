# Manifests (context & comprehension)

Place **your** manifest files here (`.list`, `.json`, or other formats you define).

- One-off local experiments: prefer `tmp/test_runs/...` unless the manifest is worth keeping long-term.
- If you introduce a new schema, add a short example file and describe required fields in this README or next to the runner in `src/tests/`.
