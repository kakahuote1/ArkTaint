# Expected outputs (optional)

Store **small** golden files or checksum sidecars for regression (e.g. normalized `context:pack` output under a fixed `--generated-at`).

Prefer hashing or minimal diffs over committing large markdown blobs. If empty, you can delete this folder; it is documented for when you add baselines.
