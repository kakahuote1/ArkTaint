# Fixtures (context & comprehension)

Static inputs only, for example:

- Minimal **State Block** JSON samples
- **Raw notes** text used with `context:pack`
- Small markdown or JSON snippets for skills / compression experiments

Keep files **small** and **non-sensitive**; large or private corpora do not belong in git.
